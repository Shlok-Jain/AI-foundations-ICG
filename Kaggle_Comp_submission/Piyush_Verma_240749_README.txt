This notebook contains a cnn model which is used for reading the sign language images and their meaning.
with the given model, I have achieved accuracy of about 93%

to install the given model, simply run the python notebook on collab or any IDE